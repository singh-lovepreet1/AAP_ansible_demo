share_parameters:
  share_name: "{{ share_name_aap }}"
  share_user: "{{ share_username_aap }}"
  share_password: "{{ share_password_aap }}"
  scp_file: "{{ scp_file_name }}"
  proxy_support: false
  ignore_certificate_warning: ignore
target: "{{ target_scp_Aap }}"
