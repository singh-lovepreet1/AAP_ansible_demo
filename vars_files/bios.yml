attributes:
  BootMode: "Bios"
  OneTimeBootMode: "Enabled"
  BootSeqRetry: "Enabled"
apply_time: "{{ apply_time_aap }}"
maintenance_window:
  start_time: "{{ maintenance_window_start_time }}"
  duration: "{{ maintenance_window_duration }}"
clear_pending: "{{ clear_pending_attr_bool }}"
reset_bios: "{{ reset_bios_bool }}"
reset_type: "graceful_restart"
job_wait: true
job_wait_timeout: 1200
